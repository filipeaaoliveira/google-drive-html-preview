# The extension itself has no dependencies. This keeps the CI actions current,
# which is the only supply chain the project has.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "chore"
